import { State } from "../states/state";

export function updateCandidate(state: State): State {
    return state;
}

export function updateEntry(state: State): State {
    return state;
}

export function updateGiven(state: State): State {
    return state;
}
